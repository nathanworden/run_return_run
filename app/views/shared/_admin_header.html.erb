<!-- Fixed admin navbar (Start) -->
<div class="d-flex sticky-top flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom box-shadow">
 <%= image_tag('plantoost-clone-logo.png', height: 35, class: 'my-0 mr-md-auto', alt: "Plantoost") %>
 <% if admin_signed_in? %>
 <nav class="">
  <a class="p-2 text-dark" href="#">Sidekiq</a>
  <a class="p-2 text-dark" href="#">Users</a>
  <a class="p-2 text-dark" href="#">Workshops</a>
 </nav>
 <% end %>
 <nav class="my-2 my-md-0 mr-md-3">
  <% if admin_signed_in? %>
   <div class="dropdown">
    <a class="btn btn-white dropdown-toggle" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
     Welcome Admin
    </a>
    <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
     <%= link_to destroy_admin_session_url, :method => :delete, class: "dropdown-item" do %>Sign out<% end %>
    </div>
   </div>
  <% else %>
   <%= link_to 'Log In', admin_session_path, class: 'btn btn-outline-primary' %>
  <% end %>
 </nav>
</div>
<!-- Fixed admin navbar (End) -->